# =============================================================================
# Sanctum Gateway Configuration
# =============================================================================
# Gateway provides optimized transaction delivery for Solana with:
# - Multi-channel routing (RPC + Jito bundles)
# - Automatic Jito tip refunds (save up to $100K+ annually!)
# - Auto compute unit calculation (no simulation needed)
# - Dynamic priority fees based on network conditions
# - Real-time observability dashboard
#
# Get your API key from: https://gateway.sanctum.so
# For Solana Cypherpunk Hackathon: Contact Sanctum team for API access
# =============================================================================

# Gateway API endpoint (default: https://gateway.sanctum.so/api)
VITE_GATEWAY_ENDPOINT=https://gateway.sanctum.so/api

# Gateway API Key - Required for full optimization and delivery features
# Without API key: Gateway works in fallback mode (standard RPC only)
# With API key: Get 99%+ success rate + cost savings + multi-channel delivery
VITE_GATEWAY_API_KEY=your_gateway_api_key_here

# Project ID for tracking and analytics (optional)
VITE_GATEWAY_PROJECT_ID=parkchain-dcp-marketplace

# Solana Network (devnet, testnet, mainnet-beta)
# For hackathon demo: Use devnet
# For production: Use mainnet-beta
VITE_SOLANA_NETWORK=devnet

# =============================================================================
# Note: Gateway Integration Benefits
# =============================================================================
# With API Key:
#   ✅ 99%+ transaction success rate (vs 85% standard)
#   ✅ Auto-refunded Jito tips (save $104K+ annually for 1000 tx/day)
#   ✅ Automatic compute unit optimization (save 15+ hrs/week)
#   ✅ Real-time observability dashboard
#   ✅ Multi-channel routing (RPC + Jito)
#
# Without API Key (Fallback Mode):
#   ⚠️ Standard Solana RPC only
#   ⚠️ No automatic optimization
#   ⚠️ No multi-channel delivery
#   ✅ Still works for demo purposes
# =============================================================================
