# ==================================
# PARKCHAIN BACKEND - ENVIRONMENT VARIABLES
# ==================================

# IMPORTANT: Fill in your Supabase credentials below
# How to get them:
# 1. Go to: https://supabase.com/dashboard
# 2. Select your project
# 3. Go to: Settings → API
# 4. Go to: Settings → Database

# ============================================
# SUPABASE CONFIGURATION (REQUIRED)
# ============================================

# Option 1: Direct Database URL (RECOMMENDED)
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# Get this from: Supabase Dashboard → Settings → Database → Connection string
# Make sure to replace [password] with your actual database password!
DATABASE_URL=postgresql://postgres:YOUR_DATABASE_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres

# Option 2: Individual Supabase Fields (Alternative)
# Get from: Supabase Dashboard → Settings → API
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
# Get from: Supabase Dashboard → Settings → Database → Database password
SUPABASE_DB_PASSWORD=your_database_password

# ============================================
# JWT AUTHENTICATION
# ============================================
# Generate a random secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# ============================================
# SERVER CONFIGURATION
# ============================================
NODE_ENV=development
PORT=3000

# ============================================
# INSTRUCTIONS TO FIND YOUR SUPABASE CREDENTIALS
# ============================================
#
# Step 1: Login to Supabase
# Go to: https://supabase.com/dashboard
#
# Step 2: Select Your Project
# Click on your Parkchain project
#
# Step 3: Get Database Connection String
# 1. Click "Settings" (bottom left)
# 2. Click "Database"
# 3. Scroll to "Connection string"
# 4. Select "URI" tab
# 5. Copy the string (it looks like):
#    postgresql://postgres:[YOUR-PASSWORD]@db.abcdefghijk.supabase.co:5432/postgres
# 6. Replace [YOUR-PASSWORD] with your actual database password
# 7. Paste it as DATABASE_URL above
#
# Step 4: Get Project Reference
# From the connection string above, your PROJECT_REF is the part after "db." and before ".supabase.co"
# Example: if connection string is db.abcdefghijk.supabase.co
# Then PROJECT_REF = abcdefghijk
#
# Step 5: Get SUPABASE_URL
# Format: https://[PROJECT_REF].supabase.co
# Example: https://abcdefghijk.supabase.co
#
# Step 6: Get SUPABASE_ANON_KEY
# 1. Click "Settings" → "API"
# 2. Find "Project API keys"
# 3. Copy the "anon public" key (starts with "eyJ...")
#
# Step 7: Get Database Password
# If you forgot your database password:
# 1. Click "Settings" → "Database"
# 2. Click "Reset database password"
# 3. Copy the new password (save it somewhere safe!)
#
# ============================================
